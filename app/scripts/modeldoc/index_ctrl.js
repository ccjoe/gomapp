define(['Slide'], function(Modal) {
    'use strict';
    return {
        init: function(page){
            page.render();
            this.createDesc();
        },
        createDesc: function(){

        }
    };
});
